#!/bin/bash
# This above line is a Shebang line starting from '#!'
# Even though it starts from # (comment-line), the kernel does not ignore it.
# It tells the operating system which interpreter to use for the following script.
# But this line should be the first line of the shell file.
# /bin/bash signifies that bash interpreter is being used
# bash == Bourne Again SHell

echo "Hello from the kernel"
echo "Hello from the bash terminal"
